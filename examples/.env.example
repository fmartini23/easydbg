# ----------------------------------------------------
#         CONFIGURAÇÃO DE AMBIENTE PARA easydbg
# ----------------------------------------------------
#
# Copie este arquivo para .env e preencha com suas credenciais.
# O arquivo .env NUNCA deve ser enviado para o repositório Git.
#
# Escolha UM dos blocos de configuração abaixo, dependendo do banco de dados
# que você está usando para desenvolvimento/teste.

# --- CLIENTE (OBRIGATÓRIO) ---
# O tipo de banco de dados a ser usado.
# Opções válidas: "postgres", "mysql", "mssql", "oracle"
DB_CLIENT=postgres

# --- CONFIGURAÇÕES COMUNS ---
# Usado pela maioria dos bancos de dados (Postgres, MySQL, etc.)
DB_HOST=localhost
DB_USER=seu_usuario
DB_PASSWORD=sua_senha_secreta
DB_DATABASE=nome_do_banco

# --- CONFIGURAÇÕES ESPECÍFICAS ---

# Porta do banco de dados (opcional, mas recomendado)
# PostgreSQL: 5432
# MySQL: 3306
# MSSQL: 1433
DB_PORT=5432

# Configuração específica para Oracle
# Geralmente, em vez de host/database, o Oracle usa uma "Connection String".
# Descomente e use a linha abaixo se estiver conectando ao Oracle.
# DB_CONNECT_STRING=localhost:1521/XEPDB1

# Configurações adicionais para MSSQL (se necessário)
# Para conexões locais ou sem SSL, pode ser necessário ajustar estas opções.
# O conector já tenta usar padrões seguros, mas você pode sobrescrevê-los.
# MSSQL_ENCRYPT=false
# MSSQL_TRUST_SERVER_CERTIFICATE=true
