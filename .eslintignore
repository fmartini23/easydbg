# Arquivo .eslintignore
#
# Lista de arquivos e diretórios que o ESLint deve ignorar.

# 1. Dependências do Node.js
# Nunca devemos verificar o código de terceiros.
node_modules/

# 2. Arquivos de build e distribuição
# Esses arquivos são geralmente gerados/minificados e não devem ser analisados.
dist/
build/
coverage/

# 3. Logs e arquivos de debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 4. Arquivos de configuração do próprio projeto
# Geralmente não precisam ser lintados, a menos que você queira.
# .eslintrc.js # Descomente se não quiser que o linter verifique sua própria config.
jest.config.js

# 5. Arquivos de ambiente
# Não contêm lógica para ser analisada.
.env
.env.example

# 6. Diretórios de cache ou temporários
.tmp/
.cache/

# 7. Arquivos específicos de IDEs
.vscode/
.idea/
